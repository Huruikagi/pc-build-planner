/**
 * 日本語ロケール向けの取り込み支援データ（表示文言ではない）。
 *
 * これは「円」表記の価格を解釈するためのトークンであり、UIに表示する文言
 * ではない。翻訳対象外である: 他ロケールでの動作を妨げない加算的な最適化
 * であり、他通貨・他表記の追加（多言語化）は本 spec の対象外である。
 *
 * パースの制御構造（`normalizer.ts` の `parsePrice`）はここへ移さず、
 * トークン（正規表現と通貨コード）だけをここへ置く。
 */

/** 「¥」記号が表す通貨コード。 */
export const YEN_SYMBOL_CURRENCY = "JPY";

/** 「1,234円」のような「円」表記の価格にマッチする正規表現。 */
export const YEN_SUFFIX_PATTERN = /^([\d,]+(?:\.\d+)?)\s*円$/;
