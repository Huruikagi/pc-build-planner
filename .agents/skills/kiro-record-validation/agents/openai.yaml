interface:
  display_name: "Validation Recorder"
  short_description: "Record successful implementation validation checkpoints"
  default_prompt: "Use $kiro-record-validation to append the latest successful feature validation to the roadmap."

policy:
  allow_implicit_invocation: false
